import 'package:flutter/material.dart';

class CashHistoryPage extends StatelessWidget {
  const CashHistoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Column(),
    );
  }
}